{
  "name": "pci-zkp",
  "version": "0.1.0",
  "private": true,
  "description": "Layer 4: Zero-knowledge proofs via Midnight for PCI",
  "type": "module",
  "scripts": {
    "build": "pnpm -r build",
    "test": "vitest",
    "test:run": "vitest run",
    "lint": "pnpm -r lint",
    "clean": "pnpm -r clean"
  },
  "devDependencies": {
    "vitest": "^2.1.0"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/peteski22/pci-zkp.git"
  },
  "keywords": [
    "pci",
    "zkp",
    "zero-knowledge",
    "midnight",
    "privacy"
  ]
}
